export default function handleLocalStorageEmailAndPassword(email, password) {
    localStorage.setItem("email", email);//saves client's data into localStorage:
    localStorage.setItem("password", password);//saves client's data into localStorage:
  } 